// //Pin feature to mount below data-fragments onto the canvas

// const [pinnedText, setPinnedText] = useState<boolean>(false);
// const toggleTextPin = () => {
//   setPinnedText((prev) => (prev === false ? true : false));
// };

// export { pinnedText, setPinnedText, toggleTextPin };

//USE PIN FEATURES IN THEIR OWN FILES | THEY MUST BE IN CONTEXT FILES
//Pin feature to mount below data-fragments onto the canvas
// const [pinnedAudio, setPinnedAudio] = useState<Record<string, boolean>>({});
// const toggleAudioPin = (id: string) => {};

// const [pinnedImage, setPinnedImage] = useState<Record<string, boolean>>({});
// const toggleImagePin = (id: string) => {};

// const [pinnedVideo, setPinnedVideo] = useState<Record<string, boolean>>({});
// const toggleVideoPin = (id: string) => {};
